pipeline {
    agent none

    stages {
        stage('Run tests') {
            agent {
                docker { image 'kmaharwal1/python-pytest:latest' }
            }
            steps {
                sh 'pytest --maxfail=1 --disable-warnings -q'
            }
        }
    }
}
